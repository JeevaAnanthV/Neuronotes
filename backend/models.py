# SQLAlchemy ORM models removed — backend now uses supabase-py (HTTPS REST API).
# Schema is managed in Supabase directly. This file is kept to avoid import errors
# in any legacy code paths that may reference it.
